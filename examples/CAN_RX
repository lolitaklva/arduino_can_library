#include <IncFile1.h>

CAN can_rx;

#define TX 4
#define RX 5

void setup() {
  can_rx.init(can_rx, RX, TX, 100);
}

void loop() {
  can_rx.readPacket();

  if(can_rx.Complete_RX()) {
      uint8_t r_data = can_rx.get_data();
  }
  delay(2000);
}
